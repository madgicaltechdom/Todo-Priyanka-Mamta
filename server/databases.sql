-- CREATE TABLE users (
--   ID SERIAL PRIMARY KEY,
--   name VARCHAR(30),
--   email VARCHAR(30)
-- );



CREATE TABLE loginuser (
  ID SERIAL PRIMARY KEY,
  name VARCHAR(30),
  url VARCHAR(30),
  email VARCHAR(30)
);
